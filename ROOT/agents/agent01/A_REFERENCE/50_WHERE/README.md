# 50_WHERE - agent mirror [agent01]

> My private view of the **WHERE** family: WHERE my work applies - locations that affect me.

**Read vs write.** I **read** the canonical homes from `shared/A_REFERENCE/50...` as read-only facets; I **write** only my own scoped notes here (`owner: role:agent01`).

**On demand.** I materialise the specific derivatives I need; by default this base note is enough.

**Status.** Empty in the template - an agent fills this after onboarding.
