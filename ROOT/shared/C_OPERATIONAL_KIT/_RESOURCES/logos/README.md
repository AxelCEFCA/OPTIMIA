# _RESOURCES / logos

Place the organisation's logo files here (SVG preferred; PNG fallback), in their variants (colour / mono / negative). Reference them from working HTML; never hard-code colours or placement - those are declared in `08_CORE_Profile` (asked via CONFIG block B).

Suggested names: `primary.svg`, `primary_mono.svg`, `program.svg`, `partner_*.svg`.
