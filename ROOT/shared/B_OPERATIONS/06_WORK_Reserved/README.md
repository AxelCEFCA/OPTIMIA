# 06_WORK_Reserved

> **Reserved** slot in the WORK family (spare / future subcategory).

In v01 this number held part of the intake/pipeline machinery; in v02 that is absorbed into `03_WORK_Flow`, so the slot is free. When this organisation needs a new WORK subcategory, define it here and declare it in `08_CORE_Profile`. Keep the canonical-home + facets law (P1).
