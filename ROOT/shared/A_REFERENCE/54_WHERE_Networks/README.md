# 54_WHERE_Networks

> **5W2H - WHERE ->** Network topology / segments.

**What goes here.** Network segments and topology (context for incidents).

**What does NOT.** The nodes -> `53`; live network state -> `11_STATUS_Now` / `03_WORK_Flow`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
