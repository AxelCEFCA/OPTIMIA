# 11_WHAT_Root

> **5W2H - WHAT ->** What the entity itself is, at the top level (the organisation/program this memory serves).

**What goes here.** The top-level charter/definition of the entity, and project charters (`project_id`).

**What does NOT.** Why it exists -> `21_WHY_Purpose`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
