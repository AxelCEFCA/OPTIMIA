# 13_WHAT_SystemTree

> **5W2H - WHAT ->** The tree of systems/products the entity owns (functional identity).

**What goes here.** System records (a projection of the CODE_CATALOG), identity & function of each system.

**What does NOT.** Where it sits -> `51_WHERE_LocationTree`; live state/lifecycle -> `11_STATUS_Now` / `12_STATUS_Daily`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
