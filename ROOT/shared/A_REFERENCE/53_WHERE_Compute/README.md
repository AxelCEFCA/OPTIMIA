# 53_WHERE_Compute

> **5W2H - WHERE ->** Compute environments (where data runs).

**What goes here.** Cloud / on-prem / HPC environments; the cloud-vs-on-prem boundary for data.

**What does NOT.** The asset -> `17_WHAT_Assets`; the building -> `51`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
