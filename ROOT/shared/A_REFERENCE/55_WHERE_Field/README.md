# 55_WHERE_Field

> **5W2H - WHERE ->** Work context: office vs field/on-site.

**What goes here.** The office/field distinction as a context tag.

**What does NOT.** The work -> `00_WORK`; when -> `62_WHEN_Schedule`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
