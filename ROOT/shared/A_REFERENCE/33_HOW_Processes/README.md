# 33_HOW_Processes

> **5W2H - HOW ->** State machines / process definitions.

**What goes here.** The definition of a lifecycle (e.g. the request lifecycle, the pipeline states).

**What does NOT.** The running instances -> `03_WORK_Flow`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
