# 14_WHAT_DataModel

> **5W2H - WHAT ->** The schema of business entities (the shape of a 'request', a ticket, an incident).

**What goes here.** Entity schemas / field definitions of the domain objects.

**What does NOT.** The valid values -> `15_WHAT_Catalogs`; instances -> WORK/STATUS.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
