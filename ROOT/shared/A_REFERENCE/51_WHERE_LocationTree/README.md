# 51_WHERE_LocationTree

> **5W2H - WHERE ->** Physical locations (buildings/sites tree).

**What goes here.** The location tree (a projection of the CODE_CATALOG).

**What does NOT.** The system there -> `13`; the macro site -> `52`.

**Status.** Empty in the template. Populate when this organisation is configured (`08_CORE_Profile` + `C_OPERATIONAL_KIT/_SKILLS/CONFIG_QUESTIONS.md`).
